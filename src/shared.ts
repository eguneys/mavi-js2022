export const w = 320
export const h = 180

const rate = 1000 / 60
export const ticks = {
  seconds: 60 * rate,
  half: 30 * rate,
  thirds: 20 * rate,
  lengths: 15 * rate,
  sixth: 10 * rate,
  five: 5 * rate,
  three: 3 * rate,
  one: 1 * rate
}

export const colors = {
  bg: 0x000000,
  red: 0xff0000,
  darkred: 0xcc0000
}
